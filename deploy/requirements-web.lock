# Exact versions captured from the RUNNING patent-bench container on 2026-08-29, while the
# pipeline was known-good (t10 mega-screen mid-run, NLM access healthy).
#
# Why this file exists: deploy/Dockerfile installed everything unpinned — requirements-web.txt
# uses >=, and both `npm install -g @anthropic-ai/claude-code` and
# `pip install notebooklm-mcp-cli` took latest. serve.sh does `docker rm -f patent-bench`
# BEFORE the new container starts, so a dependency bump in the NotebookLM CLI would take the
# whole pipeline down with no running container to fall back to. The critical two, as running:
#     notebooklm-mcp-cli        0.7.2
#     @anthropic-ai/claude-code 2.1.175
# Rollback image: patent-bench:pre-tailfix-2026-08-29
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
beautifulsoup4==4.15.0
certifi==2026.5.20
click==8.4.1
fastapi==0.136.3
gunicorn==26.0.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
lxml==6.1.1
packaging==26.2
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
python-multipart==0.0.32
PyYAML==6.0.3
soupsieve==2.8.4
starlette==1.3.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.49.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.0
